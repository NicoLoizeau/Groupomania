revoir la gestion des images avec Multer => ok
retour API, comment manipuler le DOM avec Angular sur TS ? => ok




auth.js token dans post ts lien avec sessionStorage => revoir coment envoyer les "headers"

revoir le retour de myPub, incohérent